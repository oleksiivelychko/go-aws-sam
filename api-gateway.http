POST https://{{apiId}}.execute-api.{{region}}.amazonaws.com/default/{{lambda}}
Content-Type: application/json

{
  "bucket": "{{bucket}}",
  "url": "{{url}}",
  "saveAs": "{{saveAs}}",
  "queueUrl": "{{queueUrl}}"
}
